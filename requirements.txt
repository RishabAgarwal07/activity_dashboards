# =============================================================================
# Strava Data Engineering & Visualization Project
# requirements.txt
# =============================================================================

# --- Parsing ---
gpxpy==1.6.2          # GPX file parsing
fitparse              # FIT binary file parsing
python-magic==0.4.27  # Auto-detect file type inside GZ archives
lxml                  # XML parsing for GPX files

# --- Data Processing ---
pandas==2.2.3
numpy==1.26.4
scipy==1.13.1         # Signal processing (e.g. RDP downsampling)

# --- Storage ---
pyarrow==16.1.0       # Parquet read/write
duckdb==1.0.0         # In-process SQL analytics

# --- Data Validation ---
pandera==0.20.3       # Schema enforcement and data quality checks

# --- Visualization ---
folium==0.17.0        # Interactive maps
plotly==5.22.0        # Charts and elevation profiles
altair==5.3.0         # Declarative statistical charts

# --- Dashboard ---
streamlit==1.36.0

# --- Machine Learning (activity classification) ---
scikit-learn==1.5.1

# --- Strava API (future scope) ---
# stravalib==1.5        # OAuth + Strava API wrapper

# --- Testing ---
pytest==8.2.2
pytest-cov==5.0.0     # Coverage reporting

# --- Utilities ---
pyyaml==6.0.2         # config/settings.yaml parsing
python-dateutil==2.9.0
tqdm==4.66.4          # Progress bars for batch processing
loguru==0.7.2         # Structured logging (alternative to stdlib logging)